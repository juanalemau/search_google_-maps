import { FilteEstablecimientoPipe } from './filte-establecimiento.pipe';

describe('FilteEstablecimientoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteEstablecimientoPipe();
    expect(pipe).toBeTruthy();
  });
});
